# Arguments / Parameter

def myData(a,b,c):
    print("ชื่อ = ",a," นามสกุล = ",b,"อายุ = ",c)

def addition(a,b):
    print("ผลรวม = ",a+b)

x=5
y=9
addition(x,y)

# Arguments => ค่าที่ส่งเข้าไปใน function => fname , lname , age (ตอนที่เรียกใช้ฟังก์ชั่น)
# Parameter => ค่าตัวแปรที่รับข้อมูลที่ส่งมาทำงาน (Arguments) => a , b , c

# อาส่ง -  พารับ
